language: clojure
lein: lein2
before_install:
  - npm install
  - export CHROME_BIN=chromium-browser
  - export DISPLAY=:99.0
  - sh -e /etc/init.d/xvfb start
script: lein2 doo chrome test once
cache:
  directories:
  - $HOME/.m2
